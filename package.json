{
  "name": "@jekatigr/megafon-ui",
  "private": true,
  "workspaces": [ "packages/*"],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna clean --yes",
    "publish-all": "lerna publish from-package --yes",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "pre-commit": "lerna run lint && lerna run test"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run pre-commit"
    }
  }
}
